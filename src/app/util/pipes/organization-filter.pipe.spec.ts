import { OrganizationFilterPipe } from './organization-filter.pipe';

describe('OrganizationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrganizationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
