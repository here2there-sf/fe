import { BackupFilterPipe } from './backup-filter.pipe';

describe('BackupFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BackupFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
